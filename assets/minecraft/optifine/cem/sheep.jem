{  
    "models": [
	    {
		    "part": "body",
			"id": "health_bar",
			"model": "sheep_health_bar.jpm",
			"translate": [0, -3, 0],
            "attach": "true",
			"animations": [
	            {
		           "health_bar.tz": "10 + if(is_child,12,0)",
				   "health_bar.rz": "-(player_rot_y - rot_y - body.rz)",
				   "health_bar.rx": "player_rot_x - torad(90)",
				   "health_bar.sx": "if(is_child,1.8,1)",
				   "health_bar.sy": "if(is_child,1.8,1)",
				   "health_bar.sz": "if(is_child,1.8,1)"
		        }
		    ]
		}
	]
}